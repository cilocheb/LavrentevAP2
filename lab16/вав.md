# Отчет по лабораторной работе 14 (часть 1)  
# Реляционные базы данных: создание схемы, написание SQL-запросов, индексы и анализ выполнения  

**Дата:** 05-05-2026  
**Семестр:** 2 курс 2 полугодие – 4 семестр  
**Группа:**  ПИН-б-о-24-1
**Дисциплина:** Технологии программирования  
**Студент:** Герда Никита Андреевич

## Цель работы  
Получить практические навыки работы с реляционной СУБД PostgreSQL: проектирование схемы, написание сложных запросов (JOIN, GROUP BY, подзапросы, CTE), создание индексов и анализ производительности запросов на примере базы данных интернет-магазина.

## Теоретическая часть  
Реляционные базы данных основаны на табличном представлении данных и связях через внешние ключи. В работе использовалась СУБД PostgreSQL, поддерживающая стандарт SQL, транзакции, индексы и расширенные возможности анализа запросов.  

- **JOIN** – оператор соединения таблиц по условию, в запросах применялись `JOIN ... ON` для получения данных о пользователях, заказах и товарах.  
- **GROUP BY** – группировка строк для применения агрегатных функций (`SUM`, `COUNT` и др.). Неагрегированные столбцы должны присутствовать в `GROUP BY`.  
- **HAVING** – фильтрация групп после агрегации, в отличие от `WHERE`, который применяется к отдельным строкам до группировки.  
- **CTE (Common Table Expressions)** – временные именованные подзапросы (`WITH ... AS`), улучшающие читаемость и позволяющие выполнять многошаговые операции.  
- **Индексы** – структуры, ускоряющие поиск строк по значениям одного или нескольких столбцов (например, B-tree индекс). Влияние на выполнение запроса анализируется командой `EXPLAIN ANALYZE`, показывающей реальный план выполнения.

## Практическая часть  

### Выполненные задачи  
- [x] Задача 1: Создание схемы базы данных (таблицы `users`, `products`, `orders`, `order_items`) с первичными и внешними ключами.  
- [x] Задача 2: Заполнение тестовыми данными – 2 пользователя, 3 товара, 2 заказа и 3 позиции заказов.  
- [x] Задача 3: Написание трёх аналитических запросов:
  - Запрос заказов с итоговой суммой (JOIN, GROUP BY, SUM)  
  - Отчёт по категориям с выручкой > 10000 (HAVING)  
  - Топ-3 пользователей по сумме заказов с использованием CTE  
- [x] Задача 4: Создание индекса `idx_order_items_order_id` и сравнение плана выполнения (`Seq Scan` → `Index Scan`).  

### Ключевые фрагменты кода  

**Создание таблиц (фрагмент schema.sql):**  
```sql
CREATE TABLE users (
    user_id SERIAL PRIMARY KEY,
    email VARCHAR(255) NOT NULL UNIQUE,
    full_name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE orders (
    order_id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(user_id),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(50) DEFAULT 'pending'
);
-- остальные таблицы аналогично
```

**Запрос 1 (итоговая сумма заказов):**  
```sql
SELECT 
    o.order_id,
    u.full_name,
    o.order_date,
    o.status,
    SUM(oi.quantity * oi.unit_price) AS total_amount
FROM orders o
JOIN users u ON o.user_id = u.user_id
JOIN order_items oi ON o.order_id = oi.order_id
GROUP BY o.order_id, u.full_name, o.order_date, o.status
ORDER BY total_amount DESC;
```

**Запрос 2 (выручка по категориям с HAVING):**  
```sql
SELECT 
    p.category,
    SUM(oi.quantity) AS total_sold,
    SUM(oi.quantity * oi.unit_price) AS total_revenue
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
GROUP BY p.category
HAVING SUM(oi.quantity * oi.unit_price) > 10000;
```

**Запрос 3 (топ пользователей через CTE):**  
```sql
WITH user_totals AS (
    SELECT 
        u.user_id,
        u.full_name,
        SUM(oi.quantity * oi.unit_price) AS total_spent
    FROM users u
    JOIN orders o ON u.user_id = o.user_id
    JOIN order_items oi ON o.order_id = oi.order_id
    GROUP BY u.user_id, u.full_name
)
SELECT * FROM user_totals
ORDER BY total_spent DESC
LIMIT 3;
```

**Оптимизация индексом:**  
```sql
CREATE INDEX idx_order_items_order_id ON order_items(order_id);
```

## Результаты выполнения  

### Пример работы программы  

**Результат Запроса 1:**  
```
 order_id | full_name    | order_date             | status    | total_amount
----------+--------------+------------------------+-----------+--------------
        1 | Alice Smith  | 2026-05-04 10:00:00... | completed |     75000.00
        2 | Bob Johnson  | 2026-05-04 10:01:00... | completed |      5500.00
```

**Результат Запроса 2:**  
```
 category    | total_sold | total_revenue
-------------+------------+---------------
 Электроника |          3 |      78000.00
```
(категория «Книги» исключена, т.к. 2500 < 10000)

**Результат Запроса 3:**  
```
 user_id | full_name    | total_spent
---------+--------------+-------------
       1 | Alice Smith  |    75000.00
       2 | Bob Johnson  |     5500.00
```

**Сравнение плана выполнения:**  
- *До индекса:* `Seq Scan on order_items ... (cost=0.00..1.03)`  
- *После индекса:* `Index Scan using idx_order_items_order_id on order_items ... (cost=0.14..8.15)`

### Тестирование  
- [x] Модульные тесты пройдены – все запросы отработали без ошибок и вернули ожидаемые значения.  
- [x] Интеграционные тесты пройдены – внешние ключи соблюдены, агрегации корректны.  
- [x] Производительность соответствует требованиям – замена последовательного сканирования на индексное подтверждена `EXPLAIN ANALYZE`.

## Выводы  
1. Применение `JOIN` и `GROUP BY` позволяет собирать связанные данные из нескольких таблиц и вычислять агрегатные показатели, такие как общая сумма заказа или количество проданных товаров.  
2. Фильтрация агрегированных данных должна выполняться с помощью `HAVING`, а не `WHERE`; в запросе 2 без `HAVING` мы не смогли бы оставить только категории с выручкой > 10000.  
3. Создание индекса на столбец `order_id` в таблице `order_items` изменило план выполнения с полного перебора (`Seq Scan`) на эффективный поиск по индексу (`Index Scan`), что особенно важно при больших объёмах данных.

## Скриншоты
![Скриншот 1](screens/photo_1_2026-05-16_17-47-17.jpg)
![Скриншот 2](report/screenshot2.png)
![Скриншот 3](report/screenshot3.png)

## Ответы на контрольные вопросы  

1. **В чём разница между `WHERE` и `HAVING` в агрегатных запросах?**  
   - `WHERE` фильтрует строки до группировки и не может использовать агрегатные функции.  
   - `HAVING` фильтрует группы после агрегации и может включать условия с `SUM`, `COUNT` и др.

2. **Зачем в запросе с `GROUP BY` нужно перечислять все неагрегированные столбцы?**  
   - Это требование стандарта SQL и гарантирует однозначность: для каждой группы может быть множество различных значений негруппируемого столбца. Явное указание в `GROUP BY` или использование агрегатных функций устраняет неоднозначность и предотвращает случайные результаты.

3. **Как изменится результат `EXPLAIN ANALYZE` после добавления индекса? Что означает `Seq Scan` и `Index Scan`?**  
   - После создания индекса планировщик выбирает `Index Scan` вместо `Seq Scan` для условия `WHERE order_id = 1`.  
   - `Seq Scan` – последовательное чтение всей таблицы.  
   - `Index Scan` – поиск по индексу, при котором сначала находятся физические адреса нужных строк, затем считываются только они, что значительно быстрее при точечных запросах на больших таблицах.

## Приложения  
- Исходный код:  
  - `schema.sql` – создание схемы и тестовых данных  
  - `queries.sql` – аналитические запросы  
  - `index_analysis.sql` – индекс и сравнение планов выполнения  
- Диаграммы и графики: не требуются заданием.  
- Дополнительные материалы: README с инструкциями по запуску для Windows и Linux.
